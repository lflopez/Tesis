En este sección vamos a usar la función $F(U,V,W)=(U^{r},V^{r},W^{r}),\, r\in\mathbb{N}$ para jalar a la familia de foliaciones $(\Fol{\alpha})_{\alpha\in\overline{C}}$ de $\CP$. Notemos que la función $F$ si define una función en $\CP$ pues respera las clases. Veremos que si $\apha\notin\{\, 1,j,j^{2},\infty\, \}$ la familia de foliaciones $$
